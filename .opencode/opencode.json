{
  "$schema": "https://opencode.ai/config.json",
  "default_agent": "epic-chat-setup",
  "agent": {
    "epic-chat-setup": {
      "description": "Helps gamers set up the Epic Chat desktop app — installs deps, configures Discord/Spotify API credentials, and launches the app. Use when the user says 'setup', 'install', 'configure', or 'first run'.",
      "mode": "primary",
      "model": "anthropic/claude-sonnet-4-6"
    }
  },
  "command": {
    "setup": {
      "description": "Interactive setup: install dependencies, configure API keys, and run the app."
    },
    "package": {
      "description": "Build a .exe with PyInstaller."
    }
  }
}
